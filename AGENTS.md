When asked to update configs/nix, first check the $FLOX\_\* env vars to see which files you should start editing.

When trying to debug deploying coolify services, you can use the coolify (cli). It should be locally configured.
